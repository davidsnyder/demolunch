%li.search_result{:id => "{{factual_id}}"}
  .name
    {{name}}
  .address
    {{address}} {{address_extended}}
  .locality
    {{locality}}, {{region}}
  .coordinates
    .latitude
      {{latitude}}
    .longitude
      {{longitude}} 
  .telephone
    {{tel}} 
